import 'package:flutter/material.dart';

class request_opinion extends StatelessWidget {
  const request_opinion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
