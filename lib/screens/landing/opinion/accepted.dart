import 'package:flutter/material.dart';

class accepted_opnion extends StatelessWidget {
  const accepted_opnion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
